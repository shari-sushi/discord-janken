/**
 * 1 リアクションあたり、メンション表示・取得する最大ユーザー数。
 * Embed フィールドは 1024 文字。`<@19桁id> ` ≈ 23 文字/人なので 40 人で約 920 文字、
 * 残り ~100 文字を「…ほか N 人」通知文の余白に充てる。
 * この上限(≤100)により getReactionUsers は 1 リクエストで完結し、この経路ではページングが発生しない。
 */
export const MAX_REACTION_USERS = 40
